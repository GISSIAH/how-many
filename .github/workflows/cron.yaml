name: daily-cron
on:
  schedule:
    - cron: '0 1 3 * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call apify
        run: |
          curl --request POST \
          --url 'https://how-many-two.vercel.app/api/cron' \
